# Data privacy

The original workbook is a personal health record and is intentionally not stored in this public repository.

The committed public snapshot removes calendar dates, row-level timestamps, free-text symptoms, meal labels, and other contextual fields. It retains only day-indexed aggregate counts and daily means needed to reproduce the statistical arguments in the article.

Do not add the source workbook or other row-level health exports to this directory.
